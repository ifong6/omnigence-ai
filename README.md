# omnigence-ai

## Run unit tests

`python -m unittest` means:
- `python -m` → -m flag: run a module
- `unittest` → use Python’s built-in unittest framework
- `test.test_file` → module path (package.module), **do not add `.py`**

Example:

```bash
python -m unittest test.test_file
